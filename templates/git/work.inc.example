# Copy this file to ~/.config/git/work.inc on a work machine.
# Keep employer identity, signing keys, and internal paths out of this public repository.

[user]
  name = Your Name
  email = you@company.example
  signingkey = ssh-ed25519 REPLACE_ME

[gpg]
  format = ssh

[commit]
  gpgsign = true
